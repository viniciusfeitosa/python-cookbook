AMQP_URI: 'amqp://guest:guest@rabbitmq'

DB_URIS:
    "news_service_db:Base": ${COMMANDDB_HOST}

LOGGING:
    version: 1
    handlers:
        console:
            class: logging.StreamHandler
    root:
        level: DEBUG
        handlers: [console]
